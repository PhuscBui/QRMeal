import OrderTypeLayout from '@/features/common/layouts/order-type-layout'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <OrderTypeLayout>{children}</OrderTypeLayout>
}
